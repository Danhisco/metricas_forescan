---
title: "Relatório Classificação"
author: "Danilo Pereira Mori"
date: "2026-07-27"
output: 
    bookdown::word_document2: 
        fig_caption: true              
        toc: true                      
        toc_depth: 3                   
        number_sections: true          
        keep_md: false
    bookdown::pdf_document2: 
        toc: true                      
        toc_depth: 3                    
        number_sections: true          
        latex_engine: xelatex
        keep_tex: false
editor_options: 
  chunk_output_type: console
---

```{r setup, message=TRUE,warning=TRUE, echo=FALSE, cache = FALSE, include=TRUE, eval=TRUE}
knitr::opts_chunk$set(echo = FALSE,eval = TRUE,message = FALSE,warning = FALSE,cache=TRUE,include=TRUE,
                      fig.pos = "H", fig.show = "hold",
                      dpi = 200)
library(magrittr)
library(tidyverse)
library(officer)
library(flextable)
df_ad0 <- read_csv2("G:/CCD/CVE/RESPIRATORIAS/_EQUIPE/DaniloPereiraMori/artigos/artigo_tati_NFcastingSatScan/tab_tudo_RR.csv")
```


# Initial data processing

Classification of predictions into True Positive (TP), True Negative (TN), False Positive (FP), and False Negative (FN).

```{r tratamento inicial dos dados, echo=TRUE}
df_ad <- mutate(df_ad0,classRR = ifelse(is.na(RR),0,1)) %>%
    filter(id_key %in% c("RR_casos","RR_Mdn","RR_Mdn_mv","RR_obs")) %>%
    select(COD_CIR,data,id,classRR) %>%
    pivot_wider(names_from=id,values_from=classRR) %>%
    mutate(
        Mdn_c = case_when(
            casos == Mdn & casos == 1 ~ "TP",
            casos == Mdn & casos == 0 ~ "TN",
            casos != Mdn & casos == 1 ~ "FP",
            casos != Mdn & casos == 0 ~ "FN"),
        Mdn_mv_c = case_when(
            casos == Mdn_mv & casos == 1 ~ "TP",
            casos == Mdn_mv & casos == 0 ~ "TN",
            casos != Mdn_mv & casos == 1 ~ "FP",
            casos != Mdn_mv & casos == 0 ~ "FN"),
        obs_c = case_when(
            casos == obs & casos == 1 ~ "TP",
            casos == obs & casos == 0 ~ "TN",
            casos != obs & casos == 1 ~ "FP",
            casos != obs & casos == 0 ~ "FN")
        )
```


## Métricas de classificação

```{r tab-descricao-metricas, echo=FALSE}
tb_metrics <- tibble(
  `Metric` = c(
    "Sensitivity",
    "Specificity",
    "Positive Predictive Value (PPV)",
    "Negative Predictive Value (NPV)",
    "Accuracy",
    "Balanced accuracy",
    "Youden's index"
  ),

  `Formula` = c(
    "TP / (TP + FN)",
    "TN / (TN + FP)",
    "TP / (TP + FP)",
    "TN / (TN + FN)",
    "(TP + TN) / (TP + TN + FP + FN)",
    "(Sensitivity + Specificity) / 2",
    "Sensitivity + Specificity − 1"
  ),

  `Description` = c(
    "Proportion of true positives correctly identified.",
    "Proportion of true negatives correctly identified.",
    "Probability that a positive classification corresponds to a true positive.",
    "Probability that a negative classification corresponds to a true negative.",
    "Overall proportion of correctly classified observations.",
    "Average of sensitivity and specificity.",
    "Overall ability to discriminate between positive and negative observations."
  ),

  `Interpretation` = c(
    "Ability of the method to detect RR > 1.",
    "Ability of the method to correctly RR ≤ 1.",
    "Reliability of positive classifications generated by the method.",
    "Reliability of negative classifications generated by the method.",
    "Overall percentage of classifications consistent with the reference.",
    "Summarizes performance across both classes, even when they are imbalanced.",
    "Values close to 1 indicate excellent discriminatory performance."
  )
)
#
ft_metricas <- 
  flextable(tb_metrics) |>
  theme_booktabs() |>
  # Tamanho e fonte
  fontsize(size = 10, part = "all") |>
  font(fontname = "Arial", part = "all") |>
  # Alinhamentos
  align(align = "center", part = "header") |>
  align(j = c("Metric", "Description", "Interpretation"),
        align = "left", part = "all") |>
  align(j = "Formula", align = "center", part = "all") |>
  # Cabeçalho em negrito
  bold(part = "header") |>
  # Cor de fundo do cabeçalho (opcional)
  # bg(bg = "#f0f0f0", part = "header") |>
  # Borda inferior do cabeçalho
  # border(border = fp_border(color = "black", width = 1.5), 
  #        part = "header", 
  #        border.bottom = TRUE) |>
  # Largura das colunas
  set_table_properties(layout = "autofit") |>
  # Caption sem bookmark
  set_caption("Tabela 1: Binary classification performance metrics used to evaluate nowcasting.") %>% 
  autofit()
ft_metricas
```


```{r criacao das metricas de classificao, echo=TRUE}
dfsumm <- df_ad %>%
    select(data,ends_with("_c")) %>%
    pivot_longer(ends_with("_c"),
                 names_to = "method",
                 values_to = "classe") %>%
    group_by(data) %>%
    count(method, classe) %>%
    pivot_wider(names_from = classe,
                values_from = n,
                values_fill = 0) %>%
    mutate(
        method = factor(method,
                        levels=c("Mdn_c","Mdn_mv_c","obs_c"),
                        labels=c(
                            "Standard Bayesian prediction",
                            "Moving average of Bayesian predictions",
                            "Naive baseline")),
        Sensitivity = TP/(TP+FN),
        Specificity = TN/(TN+FP),
        PPV = TP/(TP+FP),
        NPV = TN/(TN+FN),
        Accuracy = (TP+TN)/(TP+TN+FP+FN),
        BalancedAccuracy = (Sensitivity + Specificity)/2,
        Youden = Sensitivity + Specificity - 1
    )
```

```{r}
dfsumm %>% 
    select(data:FN) %>% 
    pivot_longer(cols=FP:FN) %>% 
    mutate(class_name = ifelse(name%in%c("TP","TN"),
                               "Correct classifictation",
                               "Wrong classifictation"),
           class_name = factor(class_name,
                               levels=c(
                                   "Correct classifictation",
                                   "Wrong classifictation")),
           name2 = case_when(
               grepl("N",name) ~ "RR < 1",
               grepl("P",name) ~ "RR ≥ 1")
           ) %>% 
    ggplot(aes(x=data,y=value,shape=name2,color=name2)) +
    geom_point() +
    facet_grid(method~class_name)
```


```{r resumo_das_metricas,echo=FALSE}
write_csv2(dfsumm,file = "G:/CCD/CVE/RESPIRATORIAS/_EQUIPE/DaniloPereiraMori/artigos/artigo_tati_NFcastingSatScan/metricas_classificacao.csv")
dftab <- dfsumm %>%  
    group_by(method) %>% 
    summarise(
        `Sensitivity availability, n (%)` =
            sprintf("%d (%.1f%%)",
                    sum(!is.na(Sensitivity)),
                    100 * mean(!is.na(Sensitivity))),
        across(
            c(Sensitivity,
              Specificity,
              PPV,
              NPV,
              Accuracy,
              BalancedAccuracy,
              Youden),
            ~ mean(.x, na.rm = TRUE)
            ),
        .groups = "drop"
        )
ft_metricas <- 
  flextable(dftab) |>
  theme_booktabs() |>
  # Tamanho e fonte
  fontsize(size = 10, part = "all") |>
  font(fontname = "Arial", part = "all") |>
  # Alinhamentos
  align(align = "center", part = "header") |>
  # align(j = c("Metric", "Description", "Interpretation"),
  #       align = "left", part = "all") |>
  # align(j = "Formula", align = "center", part = "all") |>
  # Cabeçalho em negrito
  bold(part = "header") |>
  # Cor de fundo do cabeçalho (opcional)
  # bg(bg = "#f0f0f0", part = "header") |>
  # Borda inferior do cabeçalho
  # border(border = fp_border(color = "black", width = 1.5), 
  #        part = "header", 
  #        border.bottom = TRUE) |>
  # Largura das colunas
  set_table_properties(layout = "autofit") |>
  # Caption sem bookmark
  set_caption("Tabela 1: Binary classification performance metrics used to evaluate nowcasting.") %>% 
  autofit()
ft_metricas
```


```{r graficos}
dfp <- dfsumm %>% pivot_longer(Sensitivity:Youden) %>% 
    mutate(name=factor(name,
                       levels=c("Sensitivity",
                                "Specificity",
                                "PPV",
                                "NPV",
                                "Accuracy",
                                "BalancedAccuracy",
                                "Youden"))
           ) %>% 
    ungroup()
p <- dfp %>% 
    filter(name %in% c("Sensitivity","Specificity","PPV","NPV")) %>% 
    ggplot(aes(x=data,y=value,color=method)) +
    scale_color_manual("",values=c(
        "Standard Bayesian prediction"="red",
        "Moving average of Bayesian predictions"="blue",
        "Naive baseline"="black")) +
    geom_line(alpha=0.4) +
    geom_point(alpha=0.4) +
    theme_light() +
    labs(y="",x="epidemiological week") +
    facet_wrap(~name,ncol=2) +
    scale_y_continuous(expand=c(0,0.01)) +
    theme(legend.position="bottom",
          strip.text=element_text(color = "black",face = "bold"),
          strip.background = element_rect(fill="white",colour = "black"))
ggsave(filename="G:/CCD/CVE/RESPIRATORIAS/_EQUIPE/DaniloPereiraMori/artigos/artigo_tati_NFcastingSatScan/metricas_clasificacao.jpg",
       plot=p,
       dpi=200,
       width=12,
       height=)
```

```{r}
dfp <- dfsumm %>% pivot_longer(Sensitivity:Youden) %>% 
    mutate(name=factor(name,
                       levels=c("Sensitivity",
                                "Specificity",
                                "PPV",
                                "NPV",
                                "Accuracy",
                                "BalancedAccuracy",
                                "Youden"))
           ) %>% 
    ungroup()
p <- dfp %>% 
    filter(name %in% c("PPV","NPV","Accuracy")) %>% 
    ggplot(aes(x=data,y=value,color=method)) +
    scale_color_manual("",values=c(
        "Standard Bayesian prediction"="red",
        "Moving average of Bayesian predictions"="blue",
        "Naive baseline"="black")) +
    geom_line(alpha=0.4) +
    geom_point(alpha=0.4) +
    theme_light() +
    labs(y="",x="epidemiological week") +
    facet_wrap(~name,ncol=3) +
    scale_y_continuous(expand=c(0,0.01)) +
    theme(legend.position="bottom",
          strip.text=element_text(color = "black",face = "bold"),
          strip.background = element_rect(fill="white",colour = "black"))
```

